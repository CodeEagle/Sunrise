import XCTest
@testable import Sunrise

final class SunriseAppTests: XCTestCase {
    func testLaunchesWithoutCrash() {
        // Smoke test — replaced once we have meaningful UI flows.
        XCTAssertNotNil(AppDelegate.self)
    }
}
